# Icon assets

The UI uses `js/icons.js` so icons can inherit each panel's color and scale cleanly. This folder is reserved for future external SVG exports (for example, individual profession badges or inventory art).
